{% form_theme serieForm 'views/form/theme/form_theme.html.twig' %}
<h2>{% block h2 %}{% endblock %}</h2>

{{ form_start(serieForm) }}
{{ form_widget(serieForm) }}
<button>{% block buttonLabel %} {% endblock %}</button>
{{ form_end(serieForm) }}
